<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <groupId>Restaurante</groupId>                   
  <artifactId>Restaurante</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>Restaurante</name>
  <url>http://maven.apache.org</url>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <java.version>21</java.version> <!-- Asegúrate de que JavaFX y MySQL son compatibles con tu versión de Java -->
  </properties>

  <dependencies>
    <!-- Dependencia JavaFX Controls -->
    <dependency>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-controls</artifactId>
        <version>23.0.1</version> <!-- Versión específica de JavaFX para Java 21 -->
    </dependency>

    <!-- Dependencia JavaFX FXML -->
    <dependency>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-fxml</artifactId>
        <version>23.0.1</version> <!-- Versión específica de JavaFX para Java 21 -->
    </dependency>

    <!-- Dependencia JavaFX Base -->
    <dependency>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-base</artifactId>
        <version>23.0.1</version> <!-- Versión específica de JavaFX para Java 21 -->
    </dependency>

    <!-- Dependencia JavaFX Graphics -->
    <dependency>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-graphics</artifactId>
        <version>23.0.1</version> <!-- Versión específica de JavaFX para Java 21 -->
    </dependency>

    <!-- Conector MySQL -->
    <dependency>
        <groupId>mysql</groupId>
        <artifactId>mysql-connector-java</artifactId>
        <version>8.0.33</version> <!-- Asegúrate de utilizar la última versión compatible -->
    </dependency>
  </dependencies>

  <build>
    <plugins>
        <!-- Plugin de compilación para Java -->
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <version>3.8.1</version>
            <configuration>
                <source>21</source>
                <target>21</target>
            </configuration>
        </plugin>

        <!-- Plugin de Spring Boot -->
        <plugin>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-maven-plugin</artifactId>
            <version>3.1.0</version>
            <configuration>
                <mainClass>Inicio.Main</mainClass> <!-- Asegúrate de que esta es la clase correcta -->
                <jvmArguments>--module-path 'C:\Program Files\Java\javafx-sdk-23.0.1\lib' --add-modules javafx.controls,javafx.fxml</jvmArguments>
            </configuration>
        </plugin>

        <!-- Plugin para ejecutar JavaFX -->
        <plugin>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-maven-plugin</artifactId>
            <version>0.0.8</version>
            <executions>
                <execution>
                    <goals>
                        <goal>run</goal>
                    </goals>
                </execution>
            </executions>
            <configuration>
                <mainClass>Inicio.Main</mainClass> <!-- Cambia esto por el nombre de tu clase principal con el paquete completo -->
                <jvmArguments>--module-path 'C:\Program Files\Java\javafx-sdk-23.0.1\lib' --add-modules javafx.controls,javafx.fxml</jvmArguments>
            </configuration>
        </plugin>
    </plugins>
  </build>
</project>